name: my-app
version: 1.0.0

database:
  host: localhost
  port: 5432
  name: mydb
  credentials:
    user: admin
    password: secret

server:
  port: 3000
  host: 0.0.0.0
  cors:
    enabled: true
    origins:
      - http://localhost:3000
      - https://example.com

features:
  auth: true
  logging: true
  cache: false
